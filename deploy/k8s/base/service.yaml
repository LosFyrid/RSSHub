apiVersion: v1
kind: Service
metadata:
  name: rss-hub-web
  labels:
    app.kubernetes.io/name: rss-hub
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rss-hub
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
