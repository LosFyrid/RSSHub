apiVersion: v1
kind: ConfigMap
metadata:
  name: rss-hub-redis-config
  labels:
    app.kubernetes.io/name: rss-hub
    app.kubernetes.io/component: redis-config
data:
  redis-additional.conf: |
    appendonly yes
